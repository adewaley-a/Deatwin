import React from "react";

// The function wrapper MUST start here
export default function GamePage() {
  
  // The return MUST be inside the function's { } braces
  return (
    <div style={{ 
      background: "#000", 
      height: "100vh", 
      color: "white", 
      display: "flex", 
      alignItems: "center", 
      justifyContent: "center",
      fontSize: "2rem",
      fontFamily: "sans-serif"
    }}>
      GAMEPAGE TEST (CLEAN)
    </div>
  );
} // The function wrapper MUST end here